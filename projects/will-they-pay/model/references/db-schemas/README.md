# DB

This folder contains actual weave table definitions.

The purpose of it is to make sure that we can easily swap the input data with data from weave, and the pipeline itself would remain unchanged.