# Output Data

This folder holds final scored outputs consumed by the service.

## Files

- `scored_predictions_prod.csv`: person-level payment likelihood predictions used at runtime.

## Quick Notes

- This is generated by `model/scripts/score_batch.py`.
- The Go server reads this file as a mock db to serve `GetPaymentLikelihood`.
- Rebuild after retraining or when source data changes.
